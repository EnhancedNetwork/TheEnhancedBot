const { MessageEmbed, MessageAttachment, AuditLogEvent } = require('discord.js');
let { joinRole } = require('../config.json')

module.exports = {
    name: 'guildMemberUpdate',
    async execute(oldMem, newMem) {

    }
}